# Tech Stack

Stack: HTML / CSS / vanilla JS (namespaced globals, script-tag concat) / Python 3 build script.

Derive fully via `map-codebase`, or `survey-context` once present. Target
architecture lives in `GENERALIZE_LATEST.md`.
